fastapi>=0.115.0
uvicorn>=0.30.1
pydantic>=2.9.0
pydantic[email]>=2.9.0          # EmailStr validator support
pydantic-settings>=2.5.0
pytest>=8.2.2
httpx>=0.27.0
# Phase 4: Database layer
sqlalchemy>=2.0.0
asyncpg>=0.29.0                  # async Postgres driver for SQLAlchemy async engine
alembic>=1.13.0                  # schema migrations
psycopg2-binary>=2.9.9           # sync driver required by Alembic env.py
pytest-asyncio>=0.23.0           # async test support
# Phase 5: Authentication & Authorization
bcrypt>=4.1.0                    # password hashing (passlib broken on Python 3.13)
PyJWT>=2.8.0                     # JWT encode/decode (python-jose not needed)
redis>=5.0.0                     # sync Redis client for rate limiting
pytest-mock>=3.12.0              # mock fixtures for rate limit and guard tests
email-validator>=2.1.0           # required by pydantic[email] for EmailStr
# Phase 10: Operational Maturity
python-multipart>=0.0.9          # required by FastAPI for file upload (UploadFile)
